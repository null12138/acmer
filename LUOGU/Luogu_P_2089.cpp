#include <bits/stdc++.h>
#define incf(i, a, b) for (int i = (a); i <= (b); i++)
#define decf(i, a, b) for (int i = (a); i >= (b); i--)
typedef long long ll;
using namespace std;
int cnt, cnt1, cnt2;

int main()
{
    int n, nn, nnn;
    cin >> n;
    nn = n % 10;
    nnn = n / 10;
    if (nnn > 300)
    {
        cout << 0;
        return 0;
    }
}